<UserControl x:Class="KP.View.CatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel ="clr-namespace:KP.ViewModel"
             xmlns:local="clr-namespace:KP.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <UserControl.DataContext>
        <viewModel:CatalogViewModel/>
    </UserControl.DataContext>
    <Grid Margin="10 0 0 0">
        <ListBox  BorderThickness="0" ItemsSource="{Binding MiniItemInfos}"
                  ItemTemplate="{StaticResource ListBoxItemTemplate}"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  ScrollViewer.VerticalScrollBarVisibility="Hidden"
                  Background="Transparent"
                  ItemContainerStyle="{StaticResource PosterContainer}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</UserControl>
